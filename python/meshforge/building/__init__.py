"""Building mode (Step 12+): floor-plan → 3D model with semantic understanding.

This subpackage is intentionally empty until Step 12-2 starts wiring
actual geometry assembly. Step 12-1 only stubs the module so the CLI mode
dispatch can import a single well-known entry point.
"""
