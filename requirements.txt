# Streamlit Community Cloud reads this file. We list dependencies directly
# instead of `.[pdf,ui]` because uv (which Streamlit Cloud prefers for
# requirements.txt) rejects local path entries, and falling back to
# pyproject.toml triggers Poetry — which cannot find our `python/meshforge`
# src layout. The meshforge package itself is loaded via a sys.path tweak
# at the top of `python/meshforge/ui_streamlit.py`.
numpy
pillow
trimesh
pymupdf
streamlit
streamlit-stl
